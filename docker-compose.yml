services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: rfp-sentinal-qdb
    ports:
      - "6333:6333"   # REST API
      - "6334:6334"   # gRPC
    volumes:
      - qdrant_data:/qdrant/storage
    restart: unless-stopped

volumes:
  qdrant_data:
    name: rfp_sentinel_qdrant_data
